// Prisma Schema - Main Configuration
// Development: Local PostgreSQL (Docker)
// Production: Supabase Cloud

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
    // directUrl = env("DIRECT_URL") // Uncomment for Supabase production with connection pooling
}
